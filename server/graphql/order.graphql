mutation createOrder($input: CreateOrderInput!){
  createOrder(input: $input) {
    table
    items{
      name
      quantity
    }
  }
}

query getOrders{
  getOrders {
    table
    floor
    status
    items{
      name
      quantity
    }
  }
}